## 1. Проектируемая архитектура (To Be)

На этапе проектирования архитектуры закладываются основные принципы, обеспечивающие расширяемость, производительность и удобство сопровождения системы.

* **Тип приложения:** REST веб-приложение с клиент-серверной архитектурой. Пользователь взаимодействует с системой через веб-интерфейс, который отправляет запросы к серверной части. Основная бизнес-логика, включая обработку данных и проверку совместимости компонентов ПК, реализуется на стороне сервера.

* **Стратегия развертывания:** Client–Server Deployment. Клиентская часть приложения (React) развёртывается как веб-интерфейс и работает в браузере пользователя. Серверная часть реализована на **Java Spring Boot** и предоставляет REST API для обработки запросов. База данных **PostgreSQL** размещается на отдельном сервере и используется для хранения информации о комплектующих и конфигурациях ПК.

* **Обоснование технологий:**
  * *Java + Spring Boot:* используются для реализации серверной части благодаря удобству разработки REST API, встроенной поддержке внедрения зависимостей и развитой экосистеме.
  * *React:* выбран для реализации пользовательского интерфейса благодаря компонентной архитектуре и высокой производительности.
  * *PostgreSQL:* используется как надежная система управления базами данных, обеспечивающая целостность и устойчивое хранение информации.

* **Показатели качества:**
  * *Масштабируемость (Scalability):* архитектура позволяет увеличивать нагрузку за счет расширения серверной части.
  * *Поддерживаемость (Maintainability):* разделение системы на уровни упрощает развитие и сопровождение проекта.
  * *Надежность (Reliability):* серверная логика централизована, что обеспечивает корректную обработку данных и ошибок.

* **Сквозная функциональность:**
  * *Логирование:* используется для отслеживания работы системы и выявления ошибок.
  * *Валидация данных:* обеспечивает корректность входных данных при обработке запросов.
  * *Обработка исключений:* централизованный механизм обработки ошибок на серверной стороне.

### Структурная схема (Функциональные блоки)

Архитектура приложения построена на основе многослойной модели и включает несколько основных уровней:

1. **Presentation Layer:** веб-интерфейс пользователя, реализованный на React.
2. **Application Layer:** REST-контроллеры Spring Boot, принимающие и обрабатывающие HTTP-запросы.
3. **Business Logic Layer:** сервисы, реализующие логику работы системы и проверку совместимости компонентов.
4. **Data Layer:** репозитории доступа к данным и база данных PostgreSQL.

![Целевая архитектура](./to_be_diagramm.png)

*Рисунок 1. Архитектура проектируемой системы (To Be)*

## 2. Анализ текущей архитектуры (As Is)

Текущая реализация системы находится на стадии активной разработки и уже включает большую часть серверной функциональности. На данный момент реализована основная структура REST-приложения: контроллеры для обработки HTTP-запросов, сервисный слой для обработки бизнес-логики и репозитории для взаимодействия с базой данных. Также настроено взаимодействие с базой данных PostgreSQL и реализованы основные сущности, описывающие комплектующие компьютера и их параметры.

На данном этапе система уже позволяет выполнять базовые операции с данными компонентов (получение информации о комплектующих, сохранение и обновление данных). Основная архитектура приложения построена на базе фреймворка Spring Boot и следует типичной многоуровневой структуре, включающей контроллеры, сервисы и репозитории.

Анализ текущего состояния проекта показывает, что основная структура приложения соответствует предполагаемой архитектуре, однако некоторые элементы пока реализованы упрощённо. Например, отдельные части бизнес-логики временно располагаются в сервисных классах без выделения специализированного модуля проверки совместимости. Это связано с тем, что разработка системы ведётся итеративно, и часть функциональности будет выделена в отдельные компоненты на следующих этапах.

![Текущая архитектура](./as_is_diagram.png) 

*Рисунок 2. Архитектурная схема текущего состояния системы (As Is)*

### 2.1. Фактическая диаграмма классов

На основе существующего исходного кода была построена диаграмма классов, отражающая реальную структуру текущей реализации системы. Диаграмма показывает основные элементы серверной части приложения, включая контроллеры, сервисы, репозитории и сущности базы данных.

Контроллеры отвечают за обработку входящих HTTP-запросов и передачу их на уровень бизнес-логики. Сервисный слой реализует основные операции работы с данными и выполняет промежуточную обработку информации. Репозитории обеспечивают доступ к базе данных и взаимодействуют с таблицами PostgreSQL через механизм ORM. Сущности описывают модели данных, используемые для хранения информации о комплектующих и конфигурациях ПК.

Полученная диаграмма показывает сервисы и сущности используемые в даднном проекте, что поможет определить дальнейшие действия для улучщения и завершения проекта

![Фактическая диаграмма классов сущностей](./Lab3_class_entity.png)

![Фактическая диаграмма классов сервисов](./Lab3_class_service.png)

*Рисунок 3-4. Детальная структура классов проекта*

## 3. Сравнение и план рефакторинга

### Сравнение «As Is» и «To Be»

Целевая архитектура предполагает чёткое разделение ответственности между слоями системы: пользовательский интерфейс, обработка запросов, бизнес-логика и доступ к данным. В модели «To Be» основная логика системы должна быть сосредоточена в сервисном слое, который будет отвечать за обработку данных и проверку совместимости комплектующих.

В текущем состоянии системы («As Is») уже реализована большая часть серверной инфраструктуры: REST-контроллеры, сервисы и репозитории, а также взаимодействие с базой данных PostgreSQL. Однако часть логики пока находится в стадии разработки. В частности, модуль проверки совместимости комплектующих ещё не выделен в отдельный компонент и пока отсутствует в архитектуре.

Кроме того, в текущей реализации отдельные сервисы выполняют одновременно несколько функций, включая обработку данных и частично управление логикой приложения. В целевой архитектуре предполагается более строгая декомпозиция компонентов, что позволит упростить развитие системы и улучшить поддерживаемость кода. На данный момент **пользовательский интерфейс отсутствует**, поэтому важной задачей является его проектирование и реализация с использованием React для веб-клиента.

### Пути улучшения (Рефакторинг)

Для приведения системы к целевой архитектуре планируется выполнить ряд архитектурных улучшений.

1. **Выделение модуля проверки совместимости.**  
   Основная бизнес-логика анализа совместимости комплектующих будет вынесена в отдельный сервисный компонент. Этот модуль будет отвечать за проверку параметров различных компонентов (например, процессора, материнской платы и оперативной памяти) и определение возможности их совместного использования.

2. **Создание пользовательского интерфейса.**  
   Разработка веб-интерфейса с использованием React позволит пользователю взаимодействовать с системой, просматривать компоненты, формировать сборки и получать результаты проверки совместимости. Интерфейс будет обращаться к REST API серверной части.

3. **Улучшение разделения слоёв приложения.**  
   Планируется более чётко разделить ответственность между контроллерами, сервисами и репозиториями. Контроллеры будут отвечать только за обработку HTTP-запросов, сервисы — за бизнес-логику, а репозитории — за работу с базой данных.

4. **Повышение модульности системы.**  
   Отдельные функциональные части приложения (например, управление компонентами и обработка конфигураций ПК) могут быть выделены в самостоятельные сервисы или модули. Это позволит упростить дальнейшее развитие системы и повысить её масштабируемость.

5. **Улучшение обработки ошибок и валидации данных.**  
   Планируется централизовать обработку исключений и использовать единый механизм проверки входных данных для REST API.

## Заключение

Проведённый анализ показал, что текущая архитектура системы уже соответствует основным принципам многослойного веб-приложения и может служить основой для дальнейшего развития проекта. Несмотря на то, что часть функциональности ещё находится в разработке, существующая структура кода позволяет относительно легко внедрить недостающие компоненты.

В частности, реализация отдельного модуля проверки совместимости комплектующих, проектирование и внедрение пользовательского интерфейса, а также улучшение разделения ответственности между слоями системы позволит приблизить архитектуру проекта к целевой модели «To Be». Это повысит поддерживаемость, расширяемость и устойчивость приложения.
